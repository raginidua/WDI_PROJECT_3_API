exit
Piece.find(pieces[0].id)
pieces[0].update!(title: "AAAA")
Piece.find(pieces[0].id)
pieces[0].update!(status: "closed")
pieces[0]
Piece.find(13)
Piece.find(7)
Piece.find(5)
pieces[0].update(status: "closed")
   41:         p "The auction for #{piece.title} has now
   40:         piece.save!
   39:         piece.update!(status: "closed")
pieces.each do |piece|
pieces = Piece.all.select { |piece| piece.closing_time < Time.now && piece.status == "open" }
